apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: ocm-int

#transformers:
#  - image-tag-transformer.yaml

patches:
  - target:
      kind: Namespace
      name: ocm
    path: patches.yaml
